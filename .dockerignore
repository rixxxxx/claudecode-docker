# Version control
.git
.gitignore

# Node
node_modules
npm-debug.log
.npm

# Python
__pycache__
*.pyc
.venv
venv

# Docker
Dockerfile
.dockerignore
docker-compose*.yml

# Secrets — never in build context
.env
.env.*
*.pem
*.key

# Exception: an optional enterprise root CA the Dockerfile trusts at build
# time (see certs/.gitkeep) — not a secret, a public certificate.
!certs/*.crt

# OS/editor cruft
.DS_Store
.vscode
.idea
*.swp

# CI/local artifacts
dist
build
*.log
