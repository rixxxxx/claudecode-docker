# Default, safe listener -- no SSL Bump capability at all. This is the
# exact pre-2026-09-18 behavior (see squid.conf's own comment). Mounted
# here unless SQUID_HTTP_PORT_DIR points at .squid-http-port-sslbump
# instead (bin/cc-container --ssl-bump).
http_port 3128
