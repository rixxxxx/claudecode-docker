# Placeholder: this workspace has no .squid-claudecode-docker/ folder of
# its own, so no per-workspace Squid extensions are configured. See
# README.md "Extending the domain allowlist" for how to add one.
